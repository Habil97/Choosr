@model int
@* Bell icon with unread badge, links to Notifications page *@
<a class="btn btn-sm btn-outline-light position-relative" asp-controller="Notifications" asp-action="Index" title="Bildirimler">
  <span aria-hidden="true">🔔</span>
  @if (Model > 0)
  {
    <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
      @((Model > 99 ? "99+" : Model.ToString()))
      <span class="visually-hidden">okunmamış bildirim</span>
    </span>
  }
</a>
